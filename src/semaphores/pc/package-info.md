This package contains code which demonstrate communication between threads

The most common example of thread communication is producer-consume problem.

This package implements producer consumer problem.